# --- Custom Keybindings for macOS Terminal ---
# Word / Line navigation keybinds
bindkey '^[[1;3C' forward-word  # Option + Right Arrow
bindkey '^[[1;3D' backward-word # Option + Left Arrow

# Word deletion key bindings
bindkey '^[[127;9u' backward-kill-word  # Cmd + Delete (delete word to the left)
bindkey '^[d' kill-word                 # Option + Delete (delete word to the right)
